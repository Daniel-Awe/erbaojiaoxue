<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.zju.temp.mapper.QuestionMapper">

    <select id="OrderAsc" resultType="cn.edu.zju.temp.entity.Question">
        select * from `question_bank` order by #{orderUo.column} ASC limit #{orderUo.limit} offset #{rows}
    </select>
    <select id="orderDesc" resultType="cn.edu.zju.temp.entity.Question">
        select * from `question_bank` order by #{orderUo.column} DESC limit #{orderUo.limit} offset #{rows}
    </select>
</mapper>